services:
  - type: web
    name: escape-finder-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: escape-finder-db
          property: connectionString
      - key: AUTO_SEED_DB
        value: "true"
      - key: CORS_ORIGINS
        value: "https://escape-finder-frontend.onrender.com,http://localhost:5173,http://127.0.0.1:5173"

  - type: web
    name: escape-finder-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        value: "https://escape-finder-backend.onrender.com"
      - key: VITE_MAPBOX_TOKEN
        sync: false

databases:
  - name: escape-finder-db
    plan: free
